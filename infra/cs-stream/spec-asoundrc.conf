# Route ALSA default device to PulseAudio so the bundled-SDL2 ALSA-only
# xash3d ends up writing into our cs_audio null sink.
pcm.!default {
    type pulse
    fallback "sysdefault"
}
ctl.!default {
    type pulse
    fallback "sysdefault"
}
